# Shared platform assets

Static files reused by **more than one** app or service under `03-platform/`.

App-only assets: `03-platform/apps/<app>/assets/`  
Package-only assets: `03-platform/packages/<pkg>/assets/`

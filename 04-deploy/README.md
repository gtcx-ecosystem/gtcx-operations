# Deploy (04-deploy)

Deploy and infrastructure artifacts for `gtcx-operations`.

| Path | Role |
| ---- | ---- |
| `docker/` | Docker images and compose (when provisioned) |
| `k8s/` | Kubernetes manifests (when provisioned) |
| `terraform/` | Terraform modules (when provisioned) |

Corporate-ops repo — minimal deploy surface today. See `01-docs/operations/` for runbooks.

**Layout v3:** Renamed from `04-ship/` (2026-06-06).

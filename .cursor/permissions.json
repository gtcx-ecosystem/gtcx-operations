{
  "terminalAllowlist": [
    "git",
    "git status",
    "git log",
    "git diff",
    "git add",
    "git commit",
    "git push",
    "git pull",
    "git fetch",
    "gh",
    "pnpm"
  ],
  "autoRun": {
    "allow_instructions": [
      "Allow git push when the user or sprint closure protocol explicitly requests pushing commits to origin.",
      "Allow gh pr create and gh pr view for pull request workflows when requested.",
      "Allow pnpm install, pnpm test, pnpm typecheck, and pnpm build for monorepo quality gates."
    ]
  }
}
